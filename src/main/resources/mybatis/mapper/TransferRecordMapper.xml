<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.biz.mapper.TransferRecordMapper">
    <resultMap id="BaseResultMap" type="com.company.project.biz.entity.TransferRecord">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="from_user_id" property="fromUserId" jdbcType="BIGINT"/>
        <result column="change_money" property="changeMoney" jdbcType="BIGINT"/>
        <result column="transaction_id" property="transactionId" jdbcType="VARCHAR"/>
        <result column="to_user_id" property="toUserId" jdbcType="BIGINT"/>
        <result column="record_no" property="recordNo" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List" >
        id, from_user_id, change_money, transaction_id, to_user_id, record_no
    </sql>

    <insert id="insert" parameterType="com.company.project.biz.entity.TransferRecord">
      insert into transfer_record
      (
      id,
      from_user_id,
      change_money,
      transaction_id,
      to_user_id,
      record_no
      )
      values
      (
      #{id,jdbcType=BIGINT},
      #{fromUserId,jdbcType=BIGINT},
      #{changeMoney,jdbcType=BIGINT},
      #{transactionId,jdbcType=VARCHAR},
      #{toUserId,jdbcType=BIGINT},
      #{recordNo,jdbcType=VARCHAR}
      )
    </insert>

    <select id="selectCount" resultType="java.lang.Integer">
        SELECT count(1)
        FROM transfer_record
        WHERE transaction_id = #{transactionId}
    </select>

</mapper>
